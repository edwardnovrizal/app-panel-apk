enum TypeRoute { replace, push, removeUntil }

enum SettingMenu {
  home,
  appInfo,
  config,
  webview,
  deeplink,
  ads,
  notifPopup,
  notifRedirect,
  blockIp,
  blockCountry,
  blockCountryCode,
  blockAsn,
  blockOrganization,
}
